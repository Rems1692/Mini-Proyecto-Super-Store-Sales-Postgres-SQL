ALTER TABLE SUPER_SOTRE_SALE_RAW
RENAME TO SUPERSTORE_RAW;

SELECT *
FROM SUPERSTORE_RAW
LIMIT 10;

SELECT COUNT(*)
FROM SUPERSTORE_RAW;

SELECT COUNT (DISTINCT CUSTOMER_ID) AS CLIENTES_UNICOS
FROM SUPERSTORE_RAW

SELECT COUNT (DISTINCT PRODUCT_ID) AS PRODUCTOS_UNICOS
FROM SUPERSTORE_RAW

SELECT COUNT (DISTINCT SUB_CATEGORY)
FROM SUPERSTORE_RAW;

SELECT ROUND(SUM (SALES),2) AS VENTAS_TOTALES
FROM SUPERSTORE_RAW

SELECT SUM (QUANTITY) AS CANTIDAD_TOTAL
FROM SUPERSTORE_RAW