1 - ¿QUÉ CATEGORÍA GENERA MÁS VENTAS Y QUE PORCENTAJE REPRESENTA?
    SELECT
    P.CATEGORY,
    ROUND(SUM(V.SALES),2) AS VENTAS_TOTALES,
    ROUND(SUM(V.SALES) * 100.0/
	SUM(SUM(V.SALES)) OVER(),0)
    AS PORCENTAJE_GANANCIAS
    FROM PRODUCTOS_SSL P
    JOIN VENTAS_SS V
    ON V.PRODUCT_ID = P.PRODUCT_ID
    GROUP BY
    P.CATEGORY
    ORDER BY
    VENTAS_TOTALES DESC;


2 - ¿QUÉ CATEGORÍA GENERA MÁS GANANCIAS Y QUE PORCENTAJE REPRESENTA?

    SELECT
    P.CATEGORY,
    ROUND(SUM(V.PROFIT),2) AS GANANCIAS_TOTALES,
    ROUND(SUM(V.PROFIT) * 100.0/
	SUM(SUM(V.PROFIT)) OVER(),0)
    AS PORCENTAJE_GANANCIAS
    FROM PRODUCTOS_SSL P
    JOIN VENTAS_SS V
    ON V.PRODUCT_ID = P.PRODUCT_ID
    GROUP BY
    P.CATEGORY
    ORDER BY
    GANANCIAS_TOTALES DESC;

3 - ¿QUÉ CATEGORÍA VENDE MÁS UNIDADES Y QUE PORCENTAJE REPRESENTA?
    SELECT 
    P.CATEGORY,
    SUM(V.QUANTITY) 
    AS UNIDADES_TOTALES,
	ROUND(SUM(V.QUANTITY) * 100.0 /
	SUM(SUM(V.QUANTITY)) OVER(),2)
    AS PORCENTAJE_UNIDADES
    FROM PRODUCTOS_SSL P
    JOIN VENTAS_SS V
    ON V.PRODUCT_ID = P.PRODUCT_ID
    GROUP BY
    P.CATEGORY
    ORDER BY
  	UNIDADES_TOTALES DESC;


4 - ¿QUÉ CATEGORÍA TIENE MÁS ÓRDENES Y QUE PORCENTAJE REPRESENTA?
    SELECT 
    P.CATEGORY,
    COUNT(DISTINCT V.ORDER_ID)
    AS CANTIDAD_ORDENES,
    ROUND(COUNT(V.ORDER_ID)* 100 /
    SUM(COUNT(V.ORDER_ID)) OVER (),2)
    AS PORCENTAJE_ORDENES
    FROM PRODUCTOS_SSL P
    JOIN VENTAS_SS V
    ON V.PRODUCT_ID = P.PRODUCT_ID
    GROUP BY
    P.CATEGORY
    ORDER BY
    CANTIDAD_ORDENES DESC;


5 - ¿QUÉ SUBCATEGORÍA VENDE MÁS?
    SELECT
    P.SUB_CATEGORY,
    ROUND(SUM(V.SALES),2) AS VENTAS_TOTALES,
    ROUND(SUM(V.SALES) * 100.0/
	SUM(SUM(V.SALES)) OVER(),2)
    AS PORCENTAJE_VENTAS
    FROM PRODUCTOS_SSL P
    JOIN VENTAS_SS V
    ON V.PRODUCT_ID = P.PRODUCT_ID
    GROUP BY
    P.SUB_CATEGORY
    ORDER BY
    VENTAS_TOTALES DESC;


6 - ¿QUÉ SUBCATEGORÍA GENERA MÁS GANANCIAS?
    SELECT
    P.SUB_CATEGORY,
    ROUND(SUM(V.PROFIT),2) AS GANANCIAS_TOTALES,
    ROUND(SUM(V.PROFIT) * 100.0/
	SUM(SUM(V.PROFIT)) OVER(),2)
    AS PORCENTAJE_GANANCIAS
    FROM PRODUCTOS_SSL P
    JOIN VENTAS_SS V
    ON V.PRODUCT_ID = P.PRODUCT_ID
    GROUP BY
    P.SUB_CATEGORY
    ORDER BY
    GANANCIAS_TOTALES DESC;

7 - ¿QUÉ SUBCATEGORÍA VENDE MÁS UNIDADES?
    SELECT 
    P.SUB_CATEGORY,
    SUM(V.QUANTITY) 
    AS UNIDADES_TOTALES,
	ROUND(SUM(V.QUANTITY) * 100.0 /
	SUM(SUM(V.QUANTITY)) OVER(),2)
    AS PORCENTAJE_UNIDADES
    FROM PRODUCTOS_SSL P
    JOIN VENTAS_SS V
    ON V.PRODUCT_ID = P.PRODUCT_ID
    GROUP BY
    P.SUB_CATEGORY
    ORDER BY
  	UNIDADES_TOTALES DESC;

8 - ¿QUÉ SUBCATEGORÍA RECIBE MÁS ÓRDENES?
    SELECT 
    P.SUB_CATEGORY,
    COUNT(DISTINCT V.ORDER_ID)
    AS CANTIDAD_ORDENES,
    ROUND(COUNT(DISTINCT V.ORDER_ID)* 100 /
    SUM(COUNT(DISTINCT V.ORDER_ID)) OVER (),2)
    AS PORCENTAJE_ORDENES
    FROM PRODUCTOS_SSL P
    JOIN VENTAS_SS V
    ON V.PRODUCT_ID = P.PRODUCT_ID
    GROUP BY
    P.SUB_CATEGORY
    ORDER BY
    CANTIDAD_ORDENES DESC;

9 - ¿QUÉ SUBCATEGORÍA TIENE EL TICKET PROMEDIO MÁS ALTO?
    SELECT 
    P.SUB_CATEGORY,
    ROUND(SUM(V.SALES)/COUNT(DISTINCT V.ORDER_ID),2) AS TICKET_PROMEDIO
    FROM PRODUCTOS_SSL P
    JOIN VENTAS_SS V
    ON V.PRODUCT_ID = P.PRODUCT_ID
    GROUP BY
    P.SUB_CATEGORY
    ORDER BY
    TICKET_PROMEDIO DESC;


10 - ¿QUÉ SUBCATEGORÍA TIENE PÉRDIDAS?
        SELECT 
    P.SUB_CATEGORY,  
    ROUND(SUM (V.PROFIT),2)
        AS PERDIDAS_TOTALES 
    FROM PRODUCTOS_SSL P
    JOIN VENTAS_SS V 
        ON V.PRODUCT_ID = P.PRODUCT_ID 
    GROUP BY 
    P.SUB_CATEGORY
	HAVING
	SUM(V.PROFIT)<0
    ORDER BY 
    PERDIDAS_TOTALES DESC

11 - TOP 10 PRODUCTOS POR VENTAS.
        SELECT 
    P.PRODUCT_NAME,  
    ROUND(SUM (V.SALES),2)
        AS VENTAS_TOTALES 
    FROM PRODUCTOS_SSL P
    JOIN VENTAS_SS V 
        ON V.PRODUCT_ID = P.PRODUCT_ID 
    GROUP BY 
    P.PRODUCT_NAME
    ORDER BY 
   	VENTAS_TOTALES DESC
	LIMIT 10;

12 - TOP 10 PRODUCTOS POR GANANCIAS.
    SELECT 
    P.PRODUCT_NAME,  
    ROUND(SUM (V.PROFIT),2)
        AS GANANCIAS_TOTALES 
    FROM PRODUCTOS_SSL P
    JOIN VENTAS_SS V 
        ON V.PRODUCT_ID = P.PRODUCT_ID 
    GROUP BY 
    P.PRODUCT_NAME
    ORDER BY 
   	GANANCIAS_TOTALES DESC
	LIMIT 10;

13 - TOP 10 PRODUCTOS POR UNIDADES.
    SELECT 
    P.PRODUCT_NAME,  
    SUM(V.QUANTITY) AS UNIDADES_TOTALES
    FROM PRODUCTOS_SSL P
    JOIN VENTAS_SS V 
        ON V.PRODUCT_ID = P.PRODUCT_ID 
    GROUP BY 
    P.PRODUCT_NAME
    ORDER BY 
   	UNIDADES_TOTALES DESC
	LIMIT 10;

14 - PRODUCTOS CON PÉRDIDAS.
            SELECT 
    P.PRODUCT_NAME,  
    ROUND(SUM (V.PROFIT),2)
        AS PERDIDAS_TOTALES 
    FROM PRODUCTOS_SSL P
    JOIN VENTAS_SS V 
        ON V.PRODUCT_ID = P.PRODUCT_ID 
    GROUP BY 
    P.PRODUCT_NAME
	HAVING
	SUM(V.PROFIT)<0
    ORDER BY 
    PERDIDAS_TOTALES 
	LIMIT 10;

15 - ¿QUE PRODUCTOS RECIBEN LOS MAYORES DESCUENTOS PROMEDIO?
    SELECT
    P.PRODUCT_NAME,
    ROUND(AVG(V.DISCOUNT)*100,2) 
    AS DESCUENTO_PROMEDIO
    FROM PRODUCTOS_SSL P
    JOIN VENTAS_SS V
    ON V.PRODUCT_ID = P.PRODUCT_ID 
    GROUP BY
    P.PRODUCT_NAME
    ORDER BY
    DESCUENTO_PROMEDIO DESC
    LIMIT 10;

16 - ¿QUE PRODUCTOS RECIBEN EL MENOR DESCUENTO PROMEDIO?
    SELECT
    P.PRODUCT_NAME,
    ROUND(AVG(V.DISCOUNT)*100,2) 
    AS DESCUENTO_PROMEDIO
    FROM PRODUCTOS_SSL P
    JOIN VENTAS_SS V
    ON V.PRODUCT_ID = P.PRODUCT_ID 
    GROUP BY
    P.PRODUCT_NAME
    ORDER BY
    DESCUENTO_PROMEDIO ASC
    LIMIT 10;

17 - ¿QUE CATEGORIA GENERA MAS INGRESOS POR UNIDAD VENDIDA?
SELECT
    P.CATEGORY,
    SUM(V.QUANTITY) AS CANTIDAD_VENDIDAS,
    ROUND(SUM(V.SALES),2) AS VENTAS_TOTALES,
    ROUND(SUM(V.SALES)/SUM(V.QUANTITY),2) AS VENTA_PROMEDIO_POR_UNIDAD
FROM VENTAS_SS V
JOIN PRODUCTOS_SSL P ON V.PRODUCT_ID = P.PRODUCT_ID
GROUP BY P.CATEGORY
ORDER BY VENTA_PROMEDIO_POR_UNIDAD DESC;

18 - ¿QUÉ CATEGORÍA GENERA MÁS INGRESO POR UNIDAD VENDIDA?
SELECT
    P.CATEGORY,
    SUM(V.QUANTITY) AS CANTIDAD_VENDIDAS,
    ROUND(SUM(V.SALES),2) AS VENTAS_T,
    ROUND(SUM(SALES)/SUM(QUANTITY),2) AS VENTA_PROMEDIO_POR_UNIDAD
FROM VENTAS_SS V
JOIN PRODUCTOS_SSL P ON V.PRODUCT_ID = P.PRODUCT_ID
GROUP BY CATEGORY
ORDER BY VENTA_PROMEDIO_POR_UNIDAD DESC;

-- Hallazgo: Technology genera ~4 veces más venta por unidad que Office Supplies.
-- Aunque Office Supplies vende casi 3.3x mas unidades, Technology genera mas
-- ingreso total porque cada unidad vale mucho mas (producto de mayor precio).